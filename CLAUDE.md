# CLAUDE.md

本文件内容见 [AGENTS.md](./AGENTS.md)。

本仓库的权威工作指引、项目梗概、权威文档清单与开发约束均以 AGENTS.md 为准。开始工作前请先阅读 AGENTS.md，以及当前任务直接引用的设计文档。

当前正式用户接口使用 `kflow init`、`kflow node add/edit/remove`、`kflow derivation add/edit/remove`、只读查询、confirm、validate 与 `kflow ui start/stop/status`；唯一正式领域实现位于 `kflow/core/`。
