# KFlow v2 重构启动指令

> 用途：将本文件作为 KFlow v2 重构工作的首个 Codex 任务说明。  
> 本文件取代此前重构计划书中与产品定义、核心模型和开发顺序有关的内容。  
> 当前阶段的首要任务不是立即编码，而是检查仓库、识别未决需求、主动追问，并形成可审查的 v2 方案。

---

## 1. 你的角色

你是 KFlow v2 重构项目的资深软件架构师和实现负责人。

你需要：

1. 阅读现有仓库、README、设计文档、测试和核心实现；
2. 区分 KFlow v1 的历史实现与仍可复用的资产；
3. 以本文件列出的产品原则为最高约束；
4. 对会影响架构、数据格式或造成大量返工的模糊需求主动追问；
5. 在需求未对齐前，不擅自补全关键产品规则；
6. 先形成领域模型和重构方案，再开始大规模编码；
7. 后续开发保持小任务、小 PR、测试先行和明确验收标准。

---

## 2. 追问规则

### 2.1 必须追问的情况

遇到以下情况时，不得自行猜测，也不得直接开始大规模编码：

- 不同解释会导致核心数据结构不同；
- 不同选择会导致持久化格式不兼容；
- 不同选择会改变 Agent 的基础工作流；
- 不同选择会显著改变知识图谱的表达能力；
- 当前仓库实现与本文件原则冲突，但无法判断应删除、迁移还是保留；
- 需求之间存在矛盾；
- 某项方案需要引入本文件尚未批准的新核心概念。

### 2.2 追问方式

追问前先检查代码和现有文档，不要询问能够从仓库直接得到的事实。

每次优先只询问一个关键问题，并包含：

- 当前发现；
- 为什么该问题影响架构；
- 2～4 个可行选项；
- 各选项的主要利弊；
- 推荐方案及理由。

不要连续抛出大量低层实现问题。优先询问会影响领域模型、用户流程和数据兼容性的决策。

### 2.3 可以自行决定的事项

以下事项可以在说明理由后自行选择：

- 模块拆分；
- 函数和内部类命名；
- 测试组织；
- 代码格式；
- 不改变外部语义的局部重构；
- 可轻易回退且不会锁定产品方向的实现细节。

---

## 3. KFlow v2 的核心定位

KFlow 面向人类与 AI Agent 的共同开发过程。

### 3.1 面向 Agent

KFlow 是一个外部的文档拓扑和影响范围管理器。

当项目中的重要文件发生变化后，KFlow 应帮助 Agent 确定：

- 哪些 Knowledge Node 已发生变化；
- 哪些下游 Knowledge Node 可能受到影响；
- 涉及哪些文件路径；
- 这些 Node 通过哪些 Derivation 相互关联；
- 当前状态是什么；
- 建议按照什么拓扑顺序检查。

KFlow 只告诉 Agent **应该关注哪里以及为什么**。

Agent 应根据当前上下文、刚刚完成的修改和任务目标，自行决定是否打开、重新读取或修改某个文件。

### 3.2 面向人类

KFlow 应为人类提供一个可理解、可导航的项目知识图谱，使用户能够：

- 快速理解重要文档之间的拓扑结构；
- 查看一次 Derivation 的输入、输出和语义；
- 从图谱跳转到相应文件；
- 查看当前哪些 Node 已变化或可能受影响；
- 理解某个文档为什么存在、由什么知识产生、影响哪些下游；
- 在 AI 快速修改项目时保持对项目结构和演化过程的掌握。

Agent 接口与人类图谱必须共享同一个核心事实模型，不能分别维护两套关系。

---

## 4. 严格的职责边界

### 4.1 KFlow 不传递文档正文

这是硬性约束。

KFlow 面向 Agent 的命令、JSON、MCP 或其他接口不得返回：

- 文档全文；
- 文档片段；
- 自动抽取的上下文文本；
- 拼装后的 Prompt；
- 为任务临时生成的文档摘要。

原因：

- 重复传递文本可能破坏 API 前缀缓存和上下文复用；
- KFlow 不知道文件是否已经存在于 Agent 上下文中；
- Agent 比 KFlow 更清楚自己是否需要重新读取；
- KFlow 应保持为低开销、确定性的元数据层。

KFlow 可以为内部变化检测读取文件、计算 hash 或读取 Git 信息，但不得把正文作为上下文输出给 Agent。

### 4.2 KFlow 不是文档编辑器

KFlow 不负责：

- 编辑 Markdown；
- 接管用户文件；
- 生成文档正文；
- 自动重写所有受影响文档；
- 替代 VS Code、Git、Codex 或 Claude Code。

### 4.3 KFlow 不是通用语义检索系统

首要能力来自显式拓扑和 Derivation，而不是向量相似度。未来可以加入辅助发现能力，但不得让隐式检索替代明确记录的知识关系。

### 4.4 KFlow 不自动判断真伪

KFlow 可以报告上游变化及潜在影响，但不应宣称某个结论一定错误、某个文档一定要修改，或 Agent 的修改已经在语义上完全正确。

---

## 5. 核心模型：Knowledge Node

### 5.1 定义

Knowledge Node 是 KFlow 管理的最小知识单元。

一个 Knowledge Node 对应：

- 一个文件；或
- 多个在知识意义上共同构成一个不可分割单元的文件。

示例：

```text
architecture
├── docs/architecture.md
└── docs/architecture.svg
```

### 5.2 硬性规则

1. 一个 Knowledge Node 可以包含一个或多个文件；
2. 一个文件只能属于一个 Knowledge Node；
3. 目录不能直接作为 Knowledge Node；
4. 不支持把章节、段落、标题或代码片段作为 Knowledge Node；
5. 一个 Knowledge Node 最多只能被一个 Derivation 产出；
6. 一个 Knowledge Node 可以作为多个 Derivation 的输入；
7. Node 的稳定身份不能仅依赖文件名，文件重命名后应尽量保持 Node 身份。

### 5.3 禁止自行扩展到章节级

不得因为担心文件粒度过粗而自行加入：

```text
docs/design.md#section-a
```

这样的章节级 Node。除非以后重新明确批准，否则 v2 只管理文件或文件集合。

---

## 6. 核心模型：Derivation

### 6.1 Derivation 是核心拓扑对象

KFlow 的关系不是普通二元边，而是一次完整的知识推导活动：

```text
零个或多个输入
        ↓
一次完整推导
        ↓
一个或多个输出
```

必须支持：

- 零输入到单输出；
- 零输入到多输出；
- 单输入到单输出；
- 单输入到多输出；
- 多输入到单输出；
- 多输入到多输出。

示例：

```text
requirements ─┐
              ├─→ [形成系统设计] ─┬→ architecture
constraints  ─┘                    └→ api-design
```

不得将它拆成多条互相独立的普通 Relation。

### 6.2 语义字段

Derivation 自身必须包含：

- `short`：简短描述；
- `detail`：详细描述。

每个 input 必须包含：

- 对应 Node；
- `short`：该输入在本次推导中的简短角色；
- `detail`：该输入在本次推导中的详细作用。

每个 output 必须包含：

- 对应 Node；
- `short`：该输出在本次推导中的简短含义；
- `detail`：该输出作为本次推导结果的详细解释。

概念示例：

```yaml
id: dv_system_design

short: 综合需求与约束形成系统设计
detail: 根据功能需求、性能目标和部署约束，共同确定系统架构与接口设计。

inputs:
  - node: requirements
    short: 提供功能目标
    detail: 定义系统必须支持的能力、输入输出和使用场景。

  - node: constraints
    short: 提供实现约束
    detail: 定义性能、资源、兼容性和部署环境限制。

outputs:
  - node: architecture
    short: 形成总体架构
    detail: 确定组件边界、数据流、运行方式和主要技术选择。

  - node: api-design
    short: 形成接口方案
    detail: 根据系统边界和使用场景确定接口及其约束。
```

### 6.3 不建立固定关系标签体系

不得把 KFlow 设计成由大量固定关系类型驱动的系统，例如：

```text
derived_from
constrained_by
implements
validated_by
supersedes
```

这些词可以出现在自由文本的 `short` 或 `detail` 中，但不应自动成为强制枚举。

KFlow 的拓扑由 Derivation 的 inputs 和 outputs 决定，语义主要由自由文本表达。只有当某个结构化字段会触发清晰、稳定且必要的机器行为时，才可以提出新增；提出前必须说明它不能由现有拓扑和自由文本表达的原因。

### 6.4 生产者唯一性

一个 Node 不能有多个生产 Derivation。

以下结构非法：

```text
Derivation A → Node X
Derivation B → Node X
```

如果 X 同时依赖两组知识，应合并为同一次 Derivation，或增加合理的中间 Node 形成分阶段推导。

### 6.5 源 Node

允许存在没有输入的源知识。

当前可以接受两种候选表达：

1. 源 Node 没有 producing Derivation；
2. 源 Node 由零输入 Derivation 产生。

如果该选择会影响 schema 和迁移，必须先追问，不得自行长期保留两套重叠语义。

---

## 7. “替代方案”不属于核心关系

不得增加 `supersedes`、`replaces` 或其他固定替代关系。

现实中的方案替代通常表现为：

1. 某个现有文件被大幅重写；
2. 原有 Derivation 元数据可能被修改；
3. 当前图谱继续描述当前项目状态；
4. 旧方案存在于 Git 和历史拓扑中。

因此：

- 替代不是一种特殊推导；
- 当前图谱不必同时保留旧方案和新方案；
- 新旧方案差异应由未来的历史系统、Git 版本和拓扑比较表达；
- 不得为了历史显示而污染当前核心模型。

---

## 8. 变化、影响与 Agent 工作流

### 8.1 修改后查询影响

标准流程：

```text
人类或 Agent 修改一个或多个文件
        ↓
Agent 调用 KFlow
        ↓
KFlow 识别发生变化的 Knowledge Node
        ↓
KFlow 返回可能受影响的下游文件路径
        ↓
Agent 按需获取 Derivation、关系语义和状态
        ↓
Agent 自行决定是否读取或修改这些文件
        ↓
Agent 对已经检查完成的 Node 执行确认命令
```

基础结果应优先返回：

- 发生变化的 Node；
- 受影响 Node 对应的文件路径；
- 直接影响和传递影响层级；
- 合理的拓扑处理顺序。

附加参数可以按需返回：

- 涉及的 Derivation；
- Derivation 的 short/detail；
- input/output 的 short/detail；
- 当前状态。

不得默认输出文档内容。

### 8.2 修改前查询前提

KFlow 应支持 Agent 在修改某个 Node 前查询其上游前提，但不负责打开或传递这些文件。

Agent 可以：

- 修改前一次性查询上游和下游，形成整体计划；
- 修改一个文件后逐层查询影响；
- 根据任务规模混合使用两种方式。

KFlow 应提供可组合查询，不强迫 Agent 使用唯一工作策略。

### 8.3 Agent 自行判断是否阅读

如果 Agent 只修改了拼写或格式，它可以查看受影响列表后判断无需重新阅读部分文件，并直接对相应 Node 执行确认。这属于 Agent 的判断，不属于 KFlow 的自动规则。

---

## 9. Node 级确认

### 9.1 确认针对 Node

确认或 reconcile 操作必须针对 Knowledge Node，而不是整个 Derivation。

概念命令可以沿用 v1：

```bash
kflow confirm <node>
```

含义：

> Agent 或人已经检查过该 Node，认为它在当前上游条件下仍然正确，或者已经完成必要修改。

不要求人工再次批准。Codex 或其他 Agent 可以独立执行确认。

### 9.2 不自动级联确认

如果：

```text
A → B → C
```

A 修改后，B 和 C 都受到影响。确认 B 只能表示 B 已检查完成，不能自动确认 C。

在多输出 Derivation 中：

```text
A、B → C、D
```

确认 C 不得自动确认 D。

### 9.3 修改 Node 时检查 Derivation

当 Agent 修改某个 Node 时，还必须检查该 Node 的 producing Derivation 是否仍然准确，包括：

- inputs 是否仍正确；
- input short/detail 是否准确；
- Derivation short/detail 是否描述当前推导；
- output short/detail 是否准确；
- 是否需要新增或移除输入；
- 同一 Derivation 的其他输出是否因此受到影响。

如果内容变化没有改变推导逻辑，不应机械修改 Derivation；如果方向、依据或形成方式发生变化，则必须同步更新元数据。

### 9.4 状态实现

外部行为可以参考 v1：

```text
修改上游 → 下游受影响
检查某 Node → confirm → 该 Node 恢复为已检查
```

内部状态模型需要重新设计。不要直接假设 `green / yellow / red` 单字段足以表达全部事实，也不要在没有真实需求前引入复杂事件溯源或过重状态系统。

---

## 10. 历史系统边界

历史功能是重要方向，但当前需求仍然模糊，不得过度设计。

### 10.1 当前原则

Git 负责保存：

- 文件内容历史；
- Node 元数据历史；
- Derivation 元数据历史。

KFlow 未来的历史界面应优先通过比较不同 Git 版本下的图谱，回答：

- 哪些 Node 被增加、删除或修改；
- 哪些 Derivation 被增加、删除或修改；
- 项目拓扑如何变化；
- 某个 Node 过去对应什么方案；
- 某次大幅重写前后的知识结构有什么区别。

### 10.2 当前禁止事项

没有明确需求前，不得自行引入：

- 完整 event sourcing；
- 全量命令日志；
- 每次扫描永久记录；
- 文档正文的重复历史；
- 复杂快照数据库；
- 时间旅行状态重放引擎；
- 专门的“方案替代事件”。

当前数据格式只需保证未来能够进行 Git 版本间的拓扑比较，不要提前完成整个历史产品。

---

## 11. 对 KFlow v1 的处理原则

### 11.1 v1 是参考，不是约束

保留并分析：

- Node / Derivation 的基本思想；
- short/detail 的语义层次；
- context / affect 查询；
- modify / confirm 的工作流；
- 图遍历和完整性检查；
- 现有测试中有效的业务规则。

不得默认继承：

- 单输出 Derivation；
- 整个 `.kflow/` 不进入 Git；
- 红黄绿单字段状态；
- 依赖人工执行 modify；
- 当前 CLI 结构；
- 当前存储格式；
- 章节级或片段级追踪。

### 11.2 不为兼容旧实现牺牲 v2

先定义正确的 v2 产品和领域模型，再决定哪些旧模块可以复用、哪些数据可以迁移、哪些命令需要废弃，以及是否需要一次性迁移工具。

---

## 12. 当前阶段的任务

收到本文件后的第一轮工作，只进行分析、追问和规划，不进行大规模重构。

### 步骤 1：检查仓库

至少检查：

- README；
- CLAUDE.md、AGENTS.md 或其他 Agent 指令；
- 产品和设计文档；
- Node、Derivation、状态、存储和图遍历实现；
- CLI；
- 测试；
- `.gitignore`；
- 打包配置；
- 最近的重要提交。

### 步骤 2：输出 v1 与 v2 差异报告

报告包含：

1. 当前实现概览；
2. 可以保留的代码和设计；
3. 与本文件冲突的设计；
4. 必须重写的模块；
5. 可以延后处理的问题；
6. 数据迁移风险；
7. 可作为 v2 验收基线的现有测试。

### 步骤 3：列出未决架构问题

只列出真正会影响领域模型或造成大量返工的问题。每个问题包括背景、影响、选项和推荐方案，并按重要性逐个向用户追问。

### 步骤 4：形成 v2 领域模型草案

在关键问题得到回答后，输出：

- Knowledge Node schema；
- Derivation schema；
- Node 与文件归属不变量；
- 生产者唯一性规则；
- 变化检测基本模型；
- 影响传播规则；
- confirm 的业务语义；
- Agent 查询结果结构；
- Git 持久化原则。

### 步骤 5：形成重构实施计划

计划必须按依赖顺序组织，不按时间限制组织，并包含阶段目标、输入、输出、验收标准、风险、测试、迁移策略和审查点。

---

## 13. 第一轮禁止执行的操作

在用户批准领域模型和重构方案前，不得：

- 重写整个项目；
- 删除 v1 代码；
- 修改默认分支；
- 大规模重命名；
- 引入数据库；
- 引入 Web UI；
- 引入 MCP；
- 引入向量数据库；
- 引入固定关系类型体系；
- 引入章节级 Node；
- 引入 event sourcing；
- 实现历史时间旅行；
- 将文档正文加入 Agent 输出；
- 为“替代方案”建立特殊关系；
- 提交未经批准的 schema。

可以创建用于说明的草案文档，但必须明确标记为 draft。

---

## 14. 第一轮完成标准

第一轮工作只有在以下条件全部满足时才算完成：

- 已检查现有仓库和核心实现；
- 已输出 v1 与 v2 的差异报告；
- 已识别真正影响架构的未决问题；
- 已对未决问题进行有依据的追问；
- 未擅自实施大规模修改；
- 已形成可供用户审查的领域模型草案；
- 已形成按依赖关系组织的重构路线；
- 所有建议均符合本文件的核心边界。

---

## 15. 北极星原则

> **KFlow 只维护重要文件、推导关系、影响状态和必要的拓扑历史。它告诉 Agent 应该关注哪些文件以及为什么，但不替 Agent 读取文件；它让人类看懂项目当前结构和演化过程，但不试图取代 Git、编辑器或 Agent。**

更简洁地说：

> **让 Agent 少扫描无关文件，让人类始终掌握项目结构。**

---

## 16. 开始执行时的首条指令

请先阅读本文件和整个仓库，然后只完成以下工作：

1. 总结当前 KFlow v1 的实际领域模型和主要工作流；
2. 列出它与本文件中 KFlow v2 原则之间的关键冲突；
3. 判断哪些模块可以复用，哪些模块应重写；
4. 找出仍然会影响 v2 核心模型的未决问题；
5. 对最优先的一个未决问题提出结构化追问。

在我回答关键问题并批准领域模型前，不要开始大规模编码。
